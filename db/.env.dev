# ============================================================================
# ⚠️ DEVELOPMENT CONFIGURATION — PostgreSQL (Local Only)
#
# This file provides environment variables for running a PostgreSQL container
# in **local development and testing environments only**.
#
# ❌ DO NOT use this configuration or credentials in production.
# ============================================================================


# ------------------------------
# 🧪 PostgreSQL Development Settings
# ------------------------------

# Username to be created for connecting to the PostgreSQL instance.
# Used by your local application or dev tools.
POSTGRES_USER=user

# Password for the above PostgreSQL user.
# ⚠️ This is for local use only — do not reuse in production.
POSTGRES_PASSWORD=password

# Name of the PostgreSQL database to be created on container startup.
POSTGRES_DB=vero-demo-db
