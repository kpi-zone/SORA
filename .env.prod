# =========================================================================
# ✅ PRODUCTION ENVIRONMENT CONFIGURATION – 8n8
#
# This file defines all necessary environment variables for running the
# 8n8 service in a **production** environment.
#
# ➤ Ensure all secrets are unique and securely generated.
# ➤ This file MUST be excluded from version control (.gitignore).
# =========================================================================

# ------------------------------
# Let's Encrypt – Default Email Setting
# ------------------------------

# Default email address used by the nginx-proxy Let's Encrypt companion container
# when no specific `LETSENCRYPT_EMAIL` is defined per service.
# ➤ Used for certificate registration and expiry notifications.
LETSENCRYPT_DEFAULT_EMAIL=matthiasm@mailbox.org

# ------------------------------
# Environment File References
# ------------------------------

# Paths to service-specific .env files used by Docker Compose or runtime processes
CUBEJS_ENV_FILE=./cubejs/.env                 # Cube.js production config
WAREHOUSE_ENV_FILE=./db/.env                  # Data warehouse connection config
MCP_ENV_FILE=./mcp-server/.env                # MCP Server environment
METABASE_ENV_FILE=./metabase/.env             # Metabase frontend environment
METABASE_DB_ENV_FILE=./metabase/.env          # Metabase DB connection (if separate)
AGNO_ENV_FILE=./agno/.env                     # Agno service environment
N8N_ENV_FILE=./n8n/.env                       # n8n automation tool environment
