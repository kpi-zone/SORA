# ========================================================================
# ⚠️ WARNING: Development Use Only
#
# This file contains root configuration values for running Vero
# in a **local or development** environment.
#
# ➤ DO NOT use this file in production.
# ➤ If sharing or committing to version control, create a redacted `.env.example`.
# ========================================================================


# ------------------------------
# MCP Server – Development Settings
# ------------------------------

# Cube.js REST API endpoint used by the MCP Server to retrieve analytics data
# Points to the v1 endpoint of Cube.js, typically exposed via Docker networking
# 'cubejs' is the Docker hostname within the sora network
MCP_ENDPOINT=http://cubejs:4000/cubejs-api/v1/

# Shared secret for authenticating MCP requests to Cube.js
# Must match CUBEJS_API_SECRET in the Cube.js environment file
MCP_API_SECRET=dev-secret-abc1234567890xyzXYZ


# ------------------------------
# Cube.js Shared Variables
# ------------------------------

# ---- SQL-over-Postgres API ----

# Optional: Exposes Cube.js via a Postgres-compatible port
# Enables integration with BI tools such as Metabase and Superset
CUBEJS_PG_SQL_PORT=15432

# Credentials for accessing the Cube.js SQL-over-Postgres API
# These do NOT relate to your actual data warehouse connection
CUBEJS_SQL_USER=username
CUBEJS_SQL_PASSWORD=password

# API secret used by Cube.js to sign and verify JWTs
# ⚠️ Replace with a strong, unique secret in any non-dev environment
CUBEJS_API_SECRET=dev-secret-abc1234567890xyzXYZ


# ------------------------------
# Environment File References
# ------------------------------

# Environment configuration file paths for various Vero services
CUBEJS_ENV_FILE=./cubejs/.env.dev           # Cube.js service
WAREHOUSE_ENV_FILE=./db/.env.dev            # Data warehouse
MCP_ENV_FILE=./mcp-server/.env.dev          # MCP Server
METABASE_ENV_FILE=./metabase/.env.dev       # Metabase frontend
METABASE_DB_ENV_FILE=./metabase/.env.dev    # Metabase database
AGNO_ENV_FILE=./agno/.env.dev               # Agno service
N8N_ENV_FILE=./n8n/.env.dev                 # n8n automation
