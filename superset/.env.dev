# ========================================================================
# ⚠️ Superset Environment Variables (Development Only)
#
# This file defines environment variables used to configure Apache Superset
# for local development — including database credentials and app secrets.
#
# ➤ DO NOT use these values in production.
# ➤ You can copy this file to `.env` or reference it in `docker-compose.yml`.
# ========================================================================

# ---------------------
# PostgreSQL Configuration
# ---------------------

# The username that will be created for Superset’s internal metadata database
POSTGRES_USER=superset

# Password for the above PostgreSQL user
POSTGRES_PASSWORD=superset

# Name of the Superset metadata database to be created at container startup
POSTGRES_DB=superset

# ---------------------
# Superset Application Settings
# ---------------------

# Secret key for Superset Flask app — used for signing session cookies, CSRF, etc.
# ⚠️ In production, generate a strong, secure key!
SECRET_KEY=eCeKi3EpACpFV5qGo/BOVMFg3HpQ1Kw/JMIZj7f63yML8SnfFHIXtgks

# SQLAlchemy database connection URL to Superset’s metadata DB
# Format: dialect+driver://username:password@host:port/database
DATABASE_URL=postgresql+psycopg2://superset:superset@db:5432/superset