# ========================================================================
# 🧪 Superset Environment Variables Template
#
# This file contains example environment variables used to configure
# Apache Superset for local development, including initial database setup
# and security settings.
#
# ➤ Copy this file and rename it to `.env` before starting your project:
#    cp .env.example .env
#
# ⚠️ This is a template. Replace placeholder values with real, secure ones
#    before deploying to any staging or production environment.
# ========================================================================

# ---------------------
# PostgreSQL Configuration
# ---------------------

# Username for Superset's internal PostgreSQL metadata database
POSTGRES_USER=superset

# Password for the PostgreSQL user
POSTGRES_PASSWORD=superset

# Name of the database that Superset will use for storing metadata
POSTGRES_DB=superset

# ---------------------
# Superset Application Settings
# ---------------------

# Secret key used by Flask for session signing, CSRF protection, etc.
# ⚠️ Replace with a secure, randomly generated value in production
SECRET_KEY=dev-secret-abc1234567890xyzXYZ

# SQLAlchemy connection string to Superset's metadata database
# Format: dialect+driver://username:password@host:port/database
DATABASE_URL=postgresql+psycopg2://superset:superset@db:5432/superset
