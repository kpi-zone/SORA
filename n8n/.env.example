# ========================================================================
# ⚠️ WARNING: Development Use Only
#
# This file contains example environment variables for local development
# and testing of n8n. Do not use these values in production environments.
# ========================================================================

# ------------------------------
# n8n Environment Variables
# ------------------------------

# Port used by the n8n service inside the Docker container
N8N_PORT=5678

# Timezone for logs, scheduling, and timestamps
GENERIC_TIMEZONE=Europe/Berlin

# Publicly accessible hostname (e.g., your local dev domain)
N8N_HOST=your-n8n-domain.local

# Protocol to access n8n — use "https" in production
N8N_PROTOCOL=http

# Secure encryption key for credentials (generate with `openssl rand -hex 32`)
N8N_ENCRYPTION_KEY=your-secure-random-encryption-key

# Webhook URL (should match protocol + host)
WEBHOOK_URL=http://your-n8n-domain.local

# ------------------------------
# Database Configuration
# ------------------------------

# Database type — supported: postgresdb, mysqldb, etc.
DB_TYPE=postgresdb

# Hostname or service name of the database server
DB_POSTGRESDB_HOST=postgres

# Database port (default for PostgreSQL is 5432)
DB_POSTGRESDB_PORT=5432

# Database name
DB_POSTGRESDB_DATABASE=n8n

# Database username
DB_POSTGRESDB_USER=n8n

# Database password
DB_POSTGRESDB_PASSWORD=n8n

# ------------------------------
# Optional: Custom Code Node Support
# ------------------------------

# List of allowed external npm modules for use in Code nodes
# NODE_FUNCTION_ALLOW_EXTERNAL=pg,qs,axios
