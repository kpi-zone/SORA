# ========================================================================
# ⚠️ WARNING: Development Use Only
#
# This file contains example credentials, API secrets, and connection
# details intended for **local development and testing purposes only**.
# DO NOT use this configuration in production.
# ========================================================================

# ------------------------------
# n8n Environment Variables
# ------------------------------

# Then 8n  basic auth configuration
N8N_BASIC_AUTH_ACTIVE=true

# The username n8n will use to connect
N8N_BASIC_AUTH_USER=admin

# The password n8n will use to connect
N8N_BASIC_AUTH_PASSWORD=securepassword

# The port on which the n8n server will listen inside the Docker container.
# This is the internal container port (default is 5678).
N8N_PORT=5678

# The timezone used by n8n for logging, scheduling workflows, and timestamps.
# This should be set to match your local or desired operational timezone.
GENERIC_TIMEZONE=Europe/Berlin

# The externally accessible hostname or domain where your n8n instance will be available.
# In production, replace this with a real domain name or IP.
N8N_HOST=127.0.0.1

# The protocol (http or https) used to access the n8n web interface and webhooks.
# Always use https in production environments.
N8N_PROTOCOL=https

# This key is used for encrypting sensitive data (like credentials) stored in the n8n database.
# It must be kept secret and safe. Losing this key will make encrypted data unrecoverable.
N8N_ENCRYPTION_KEY=bdb05abc1b76de69b75c473267fb4e9816aafc8a79ff1bf75f612a150554f8d9

# The base URL n8n uses when generating webhook endpoints.
# It should align with N8N_HOST and N8N_PROTOCOL.
WEBHOOK_URL=http://127.0.0.1

# ------------------------------
# Database Configuration
# ------------------------------

# The type of database used by n8n. Options include 'postgresdb', 'mysqldb', etc.
DB_TYPE=postgresdb

# The hostname or service name of the PostgreSQL server.
# For Docker Compose, this would typically be the name of the database service.
DB_POSTGRESDB_HOST=n8n_db

# The port on which the PostgreSQL database is listening.
# Default for PostgreSQL is 5432.
DB_POSTGRESDB_PORT=5432

# The name of the database used by n8n.
DB_POSTGRESDB_DATABASE=n8n

# The username used to authenticate with the PostgreSQL database.
DB_POSTGRESDB_USER=n8n

# The corresponding password for the database user above.
DB_POSTGRESDB_PASSWORD=n8n

# ------------------------------
# Optional: Custom Code Node Support
# ------------------------------

# Uncomment and list allowed external npm modules if you're using custom functions in Code nodes.
# This enables n8n to import and use external libraries like pg, qs, axios, etc.
# NODE_FUNCTION_ALLOW_EXTERNAL=pg,qs,axios
