# ========================================================================
# ⚠️ WARNING: Development Use Only
#
# This file contains example credentials, API secrets, and connection
# details intended for **local development and testing purposes only**.
# ➤ DO NOT use this configuration in production.
# ========================================================================


# ------------------------------
# NGINX Virtual Host Configuration
# ------------------------------

# The virtual hostname used for proxying (e.g., by NGINX or Traefik)
VIRTUAL_HOST=n8n.localhost

# The internal port n8n listens on, forwarded by the proxy
VIRTUAL_PORT=5678


# ------------------------------
# n8n Application Settings
# ------------------------------

# Enable basic authentication for accessing the n8n UI
N8N_BASIC_AUTH_ACTIVE=true

# Username for basic authentication
N8N_BASIC_AUTH_USER=admin

# Password for basic authentication
N8N_BASIC_AUTH_PASSWORD=securepassword

# The internal port n8n listens on inside the Docker container
N8N_PORT=5678

# Timezone used for logs, schedules, and timestamps
GENERIC_TIMEZONE=Europe/Berlin

# Publicly accessible host or IP for the n8n instance
# Replace with a real domain or IP in production
N8N_HOST=127.0.0.1

# Protocol used to serve the n8n instance (http or https)
# ⚠️ Use HTTPS in production
N8N_PROTOCOL=https

# Encryption key used to protect sensitive data in the database
# 🔐 Must be securely stored — losing it will make encrypted data unrecoverable
N8N_ENCRYPTION_KEY=bdb05abc1b76de69b75c473267fb4e9816aafc8a79ff1bf75f612a150554f8d9

# The base URL used to generate webhook endpoints
# Should match the combination of N8N_PROTOCOL and N8N_HOST
WEBHOOK_URL=http://127.0.0.1

# Enable health check endpoint at /healthz
N8N_HEALTH_CHECK_ACTIVE=true

# Use secure cookies for sessions
# ⚠️ Set to true in production when using HTTPS
N8N_SECURE_COOKIE=false


# ------------------------------
# Database Configuration
# ------------------------------

# Type of database used (e.g., postgresdb, mysqldb)
DB_TYPE=postgresdb

# Hostname of the PostgreSQL server (typically the service name in Docker)
DB_POSTGRESDB_HOST=n8n_db

# Port for PostgreSQL (default: 5432)
DB_POSTGRESDB_PORT=5432

# Name of the n8n database
DB_POSTGRESDB_DATABASE=n8n

# Database username
DB_POSTGRESDB_USER=n8n

# Database password
DB_POSTGRESDB_PASSWORD=n8n


# ------------------------------
# PostgreSQL Init Variables (Optional)
# ------------------------------

# These are typically used by Docker images to initialize the database
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n
POSTGRES_DB=n8n


# ------------------------------
# Optional: Code Node External Modules
# ------------------------------

# Allow importing external npm packages in Function and Code nodes
# Uncomment and specify a comma-separated list of modules if needed
# NODE_FUNCTION_ALLOW_EXTERNAL=pg,qs,axios
