# ========================================================================
# ⚠️ WARNING: Development Use Only
#
# This file contains example credentials, API secrets, and connection
# details intended for **local development and testing purposes only**.
# ➤ DO NOT use this configuration in production.
# ========================================================================


# ------------------------------
# NGINX Virtual Host Configuration
# ------------------------------

# The virtual hostname used for local development (e.g., via NGINX or Traefik)
VIRTUAL_HOST=metabase.localhost

# The internal port Metabase listens on, used by the proxy
VIRTUAL_PORT=3000


# ------------------------------
# Metabase Environment Variables
# ------------------------------

# Database type Metabase will use to store its internal metadata
# Typically 'postgres', but could also be 'mysql' or 'h2' (not recommended)
MB_DB_TYPE=postgres

# Name of the internal Metabase database (not your analytics data source)
MB_DB_DBNAME=metabase

# Port the Postgres database is exposed on (default is 5432)
MB_DB_PORT=5432

# Username Metabase will use to authenticate with the database
MB_DB_USER=user

# Password for the above database user
MB_DB_PASS=password

# Hostname or Docker service name of the database container
MB_DB_HOST=metabase-db


# ------------------------------
# PostgreSQL Container Variables
# ------------------------------

# Name of the PostgreSQL database to initialize on container startup
POSTGRES_DB=metabase

# Default PostgreSQL user to be created
POSTGRES_USER=user

# Password for the default PostgreSQL user
POSTGRES_PASSWORD=password
